[railway]
language = "node"

[build]
command = "cd backend && npm install && npm run db:migrate"

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
NODE_ENV = "production"