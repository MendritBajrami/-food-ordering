{
  "name": "food-ordering-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend || npm install",
    "build": "npm install --prefix backend || npm install",
    "start": "node src/index.js || node backend/src/index.js"
  }
}
